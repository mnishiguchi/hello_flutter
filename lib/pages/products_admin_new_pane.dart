import 'package:flutter/material.dart';

class ProductsAdminNewPane extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('New Product'),
    );
  }
}
