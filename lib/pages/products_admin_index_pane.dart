import 'package:flutter/material.dart';

class ProductsAdminIndexPane extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('All Products'),
    );
  }
}
